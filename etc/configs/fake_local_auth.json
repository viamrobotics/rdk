{
    "network": {
        "fqdn": "something-unique",
        "bind_address": ":8080"
    },
    "auth": {
        "handlers": [
            {
                "type": "api-key",
                "config": {
                    "key": "${RDK_AUTH_API_KEY}"
                }
            }
        ]
    },
    "components": [
        {
            "type": "arm",
            "model": "fake",
            "name": "arm1"
        },
        {
            "type": "servo",
            "model": "fake",
            "name": "servo1"
        }
    ]
}
