{
    "network": {
        "fqdn": "something-unique",
        "bind_address": ":8080"
    },
    "auth": {
        "handlers": [
            {
                "type": "api-key",
                "config": {
                    "key": "${RDK_AUTH_API_KEY}"
                }
            }
        ]
    },
    "components": [
        {
            "name": "arm1",            
            "type": "arm",
            "model": "fake"
        },
        {
            "name": "servo1",
            "type": "servo",
            "model": "fake"

        }
    ]
}
