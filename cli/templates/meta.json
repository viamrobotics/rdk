{
    "module_id": "{{.Namespace}}{{.ModuleName}}",
    "visibility": "public",
    "description": "Modular {{.Resource}}: {{.ModelName}}",
    "models": [
        {
            "api": "rdk:{{.ResourceSubtype}}:{{.ResourceType}}",
            "model": "{{.Namespace}}:{{.ModuleName}}:{{.ModelName}}"
        }
    ],
    "entrypoint": "{{if eq .Language "python" }}run.sh{{else}}bin/main{{end}}"
}